<UserControl x:Class="AppUI.Views.MarkerPageHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppUI.Views"
             xmlns:vm="clr-namespace:AppUI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="1920">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <Style TargetType="TextBlock" BasedOn="{StaticResource TextBlockBase}">
            <Setter Property="FontSize" Value="20"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Margin="3,0"
              Visibility="{Binding IsPatientInfoVisible, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="患者ID" HorizontalAlignment="Right" />
            <TextBlock Grid.Row="0" Grid.Column="1" Text="：" />
            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding PatientId}" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="患者名" HorizontalAlignment="Right" />
            <TextBlock Grid.Row="1" Grid.Column="1" Text="：" />
            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding PatientName}" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="撮影日時" HorizontalAlignment="Right" />
            <TextBlock Grid.Row="2" Grid.Column="1" Text="：" />
            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding StudyDate}" />
        </Grid>

        <Grid Grid.Column="1"
              Visibility="{Binding IsProgressVisible, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
            <ContentControl Content="{Binding StateProgressViewModel}" />
        </Grid>

        <Grid Grid.Column="2" HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" >
                <Button VerticalAlignment="Stretch" Margin="8,0"
                        Style="{StaticResource RoundedButtonStyleShort}"
                        Visibility="{Binding IsProgressVisible, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"
                        Command="{Binding HomeButtonCommand}">ホーム</Button>
                <!--<Button VerticalAlignment="Stretch" Margin="8,0"
                        Style="{StaticResource RoundedButtonStyleShort}"
                        Command="{Binding MinimizeButtonCommand}">最小化</Button>
                <Button VerticalAlignment="Stretch" Margin="8,0"
                        Style="{StaticResource RoundedButtonStyleShort}"
                        Command="{Binding EndButtonCommand}">終了</Button>-->
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
